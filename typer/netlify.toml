[build]
# The command Netlify runs to build your Vite project
command = "npm run build"

# The directory containing the compiled production files
publish = "dist"

[[redirects]]
# Fixes 404 errors on page refresh if using React/Vue/Svelte routers
from = "/*"
to = "/index.html"
status = 200
